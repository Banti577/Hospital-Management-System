<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <!-- Google Fonts for better typography -->
    <link href="https://fonts.googleapis.com/css?family=Roboto:400,700&display=swap" rel="stylesheet" />
    <!-- Bootstrap 5 CDN for modern styling -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
    <style>
      body {
      font-family: 'Roboto', Arial, sans-serif;
      background: #f8f9fa;
      }
      .navbar {
      margin-bottom: 2rem;
      }
      .card {
      box-shadow: 0 2px 8px rgba(0,0,0,0.08);
      transition: transform 0.2s;
      }
      .card:hover {
      transform: translateY(-5px) scale(1.03);
      box-shadow: 0 4px 16px rgba(0,0,0,0.12);
      }
      .card-img-top {
      height: 180px;
      object-fit: cover;
      }
      .container.mt-4 {
      min-height: 60vh;
      }
      .no-blogs {
      text-align: center;
      padding: 4rem 0;
      color: #6c757d;
      }
    </style>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <%- include('partial/head.ejs') %>
    <title>HomePage</title>
  </head>
  <body>
    <%- include('partial/navbar.ejs') %>

    <div class="container mt-4 d-flex flex-wrap justify-content-center gap-3">

    <% if(blogs && blogs.length > 0) { blogs.forEach(blog => { %>

    <div class="card" style="width: 18rem">
      <img
        src="<%= blog.coverImageUrl %>"
        class="card-img-top"
        alt="Blog Image"
      />

      <div class="card-body">
        <h5 class="card-title"><%= blog.title %></h5>
        <a href="/blog/<%= blog._id %>" class="btn btn-primary">Go somewhere</a>
      </div>

    </div>

  
    <% }) %> <% } else { %>
    <div class="container mt-4">
      <h1>No Blogs Available</h1>
      <p>Please check back later or add a new blog.</p>
    </div>
    <% } %> 
   </div>

   
    <%- include('partial/script.ejs') %>
    <!-- Bootstrap JS Bundle (includes Popper) for carousel functionality -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
